stages:
  - build
 
compile:
  stage: build
  tags:
    - windows
  script:
    - cd .\\examples\\modbus\\sunrise_modbus_continuous
    - dir
    - arduino-cli compile --fqbn arduino:avr -o ignored0.bin sunrise_modbus_continuous.ino
