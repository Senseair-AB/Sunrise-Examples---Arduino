stages:
  - build
 
compile:
  stage: build
  tags:
    - windows
  script:
    - cd .\\examples\\modbus\\sunrise_modbus_continuous
    - arduino-cli compile -o ignored0.bin sunrise_modbus_continuous.ino
